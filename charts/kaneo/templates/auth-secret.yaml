{{- if not .Values.kaneo.env.existingSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kaneo.fullname" . }}-auth
  labels:
    {{- include "kaneo.labels" . | nindent 4 }}
type: Opaque
stringData:
  auth-secret: {{ .Values.kaneo.env.authSecret | quote }}
{{- end }}
