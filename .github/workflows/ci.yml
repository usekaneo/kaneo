name: Code quality

on:
  push:

jobs:
  quality:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Biome
        uses: biomejs/setup-biome@v2
        with:
          version: 2.4.8
      - name: Run Biome
        run: biome ci .
